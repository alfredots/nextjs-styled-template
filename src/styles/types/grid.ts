export type GridProps = {
  gap: string;
  columnGap: string;
  rowGap: string;
  gridColumn: string;
  gridRow: string;
  gridAutoFlow: string;
  gridAutoColumns: string;
  gridAutoRows: string;
  gridTemplateColumns: string;
  gridTemplateAreas: string;
  gridArea: string;
};
